{
  "name": "contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "echo \"No tests configured\"",
    "compile": "node scripts/compile.js",
    "deploy": "node scripts/direct-deploy.js",
    "setup-mock": "node scripts/setup-mock.js",
    "verify": "echo \"No verification configured\""
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  },
  "devDependencies": {
    "chai": "^4.3.8",
    "dotenv": "^16.3.1",
    "ethers": "^6.9.0",
    "solc": "^0.8.30"
  }
}